<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sir.app">

    <application
        android:name=".base.common.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_versions"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:name="design_width" android:value="768"></meta-data>
        <meta-data android:name="design_height" android:value="1280"></meta-data>
        <activity
            android:name=".DetailsActivity"
            android:label="Toolbar"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".TabLayoutAcivity"
            android:label="TabLayout"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".HideBarActivity"
            android:label="HideBar"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".FABToolActivity"
            android:label="FABTool"></activity>
        <activity
            android:name=".RecyclerViewActivity"
            android:label="RecyclerView"></activity>
    </application>

</manifest>
