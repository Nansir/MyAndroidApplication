<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sir.app.panel">

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width"
            android:value="1080"></meta-data>

        <meta-data
            android:name="design_height"
            android:value="1920"></meta-data>

        <activity
            android:name=".SlidingActivity"
            android:label="Sliding"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ResideActivity"
            android:label="Residing"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".NavigationActivity"
            android:label="Navigation"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".SlidingRootActivity"
            android:label="SlidingRoot"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
